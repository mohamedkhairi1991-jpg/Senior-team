run = "cd backend && npm install && npm run dev"
modules = ["nodejs-20"]

[agent]
expertMode = true

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Production App"

[[workflows.workflow]]
name = "Production App"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd backend && NODE_ENV=production PORT=5000 npm start"
waitForPort = 5000

[workflows.workflow.metadata]
outputType = "webview"

[[ports]]
localPort = 3001
externalPort = 3001

[[ports]]
localPort = 5000
externalPort = 80

[deployment]
deploymentTarget = "autoscale"
run = ["bash", "-c", "cd backend && NODE_ENV=production PORT=5000 npm start"]
build = ["bash", "-c", "cd web && npm install && npm run build && cd ../backend && npm install && npm run build"]
