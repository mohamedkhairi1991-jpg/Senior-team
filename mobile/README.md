# Hospital Coordination Mobile (Expo/React Native)

To run this app:
- Install Expo Go on your device.
- Run `npm install` then `expo start` in this folder.
- Scan the QR code to open on your device.
